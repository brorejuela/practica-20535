#include <iostream>

using namespace std;


double dividir(double a, double b) {
    if (b == 0) {
        cout<< "Error: No se puede dividir entre cero." << endl;
        return 0;
    }
    return a / b;

    return 0;
}
